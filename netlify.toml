[build]
  # The directory where your functions are located
  functions = "netlify/functions"

[functions]
  # Ensure the functions are bundled correctly
  node_bundler = "esbuild"

[[redirects]]
  # This helps handle routing if you use a custom domain
  from = "/*"
  to = "/index.html"
  status = 200